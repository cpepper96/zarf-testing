kind: ZarfPackageConfig
metadata:
  name: podinfo-test
  description: A real deployable Zarf package with podinfo for testing
  version: 1.0.0

components:
  - name: podinfo
    required: true
    manifests:
      - name: podinfo-deployment
        files:
          - deployment.yaml
          - service.yaml
    images:
      - stefanprodan/podinfo:6.3.0
